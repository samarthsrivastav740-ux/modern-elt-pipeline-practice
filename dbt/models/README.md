# Models

The models directory contains the SQL transformations built using dbt.

The project follows a layered approach:

- Staging Models
- Dimension Models
- Fact Models

Each layer has a specific responsibility in preparing the data for analytics.
